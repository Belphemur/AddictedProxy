using AddictedProxy.Model.Config;

namespace AddictedProxy.Services.Addic7ed.EnvVar.Credentials;

public class MainCreds : Addic7edCreds
{
    public MainCreds(long userId, string password) : base(userId, password)
    {
    }
}